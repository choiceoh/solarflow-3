{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "_comment": "AGENT-BUILDER-VISION v1 가드레일. Pre/PostToolUse 가 .claude/hooks/domains.json lookup 후 도메인별 blast_radius / verify_scripts 를 stdout JSON {hookSpecificOutput.additionalContext} 로 주입. v1 advisory (exit 0). PR-B 부터 STRICT. Exec form (args) 사용 — cwd 의존성 제거 + Windows 호환 (.cmd shim 회피).",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "node",
            "args": ["${CLAUDE_PROJECT_DIR}/.claude/hooks/pre-edit.mjs"]
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "node",
            "args": ["${CLAUDE_PROJECT_DIR}/.claude/hooks/post-edit.mjs"]
          }
        ]
      }
    ]
  }
}
