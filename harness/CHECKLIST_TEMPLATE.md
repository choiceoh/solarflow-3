# 감리 체크리스트 표준 양식

시공자는 모든 작업 완료 후 이 양식으로 보고할 것.

## 기본 정보
- 작업명:
- 생성/수정 파일:
- go build ./... 결과:
- go vet ./... 결과:

## 체크리스트
| 항목 | O/X | 비고 |
|------|-----|------|
| struct 타입 (map[string]interface 금지) | | |
| 에러 처리 (json.Unmarshal, DB 에러 전부) | | |
| 입력값 검증 (Validate 메서드) | | |
| 설계문서 필드 전부 반영 | | |
| 에러 응답 형식 (response.RespondError) | | |
| 인증 체크 | | 미구현이면 "X — 미구현" |
| Rust 분리 (TODO 주석) | | |
| 코드 주석 한국어 | | |
| nullable 필드 포인터 타입 | | |
| 양수 검증 (수량/금액/크기) | | |

## 자체 평가
- 점수: /10
- 감점 이유:

## 전체 코드
(cat으로 전체 파일 내용 첨부 필수)
