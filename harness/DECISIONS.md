# SolarFlow 설계 판단 기록

모든 주요 설계 판단의 "왜?"를 기록합니다.
새 대화창에서 "왜 이렇게 했지?"라는 질문이 나오면 이 문서를 참조합니다.

## D-001: Go+Rust 아키텍처 분리
- **결정**: Go(프론트+API) + Rust(계산엔진)
- **이유**: Go는 변경이 잦은 UI/API에 적합, Rust는 복잡한 계산(Landed Cost, 재고집계)에 안정적
- **분리 기준**: 한 행 사칙연산=Go, 여러 테이블 조합=Rust
- **날짜**: 2026-03

## D-002: 엑셀 기반 입력 방식 채택
- **결정**: SolarFlow = 입력 시스템 → 엑셀 Import/Export 허브 + 분석 시스템
- **이유**: (1) SolarFlow 다운 시 엑셀 원본이 실무자 PC에 있어 업무 마비 없음 (2) 디원/화신은 ERP 없어서 SolarFlow가 유일한 시스템, 다운 리스크 큼 (3) 실무자 업무 방식 변경 최소화
- **날짜**: 2026-03-29

## D-003: ToggleStatusRequest를 common.go에 배치
- **결정**: model/company.go가 아닌 model/common.go에 배치
- **이유**: is_active 토글은 company, manufacturer, product 등 여러 핸들러에서 공통 사용
- **날짜**: 2026-03-29

## D-004: 감리자-시공자 분리 도입
- **결정**: 코드 작성(시공자)과 설계 검증(감리자)을 별도 대화창으로 분리
- **이유**: 시공자(Claude)가 3번의 대화창에서 반복적으로 "Go만으로 충분" 하며 설계를 임의 변경. 감리자가 설계문서 대비 코드를 대조하여 이 패턴을 차단.
- **날짜**: 2026-03-28

## D-005: Validate()가 error가 아닌 string 반환
- **결정**: Validate() 메서드가 Go 관례(error)가 아닌 string 반환
- **이유**: 시공자의 초기 구현. 동작에 문제없고 모든 핸들러에 일관 적용됨. 전체 변경 비용 대비 이득이 적어 유지.
- **날짜**: 2026-03-29

## D-006: 자동 검증 3층 구조 도입
- **결정**: (1층) go build+vet+test+CI+린터 (2층) 감리자 대화창 (3층) 상사 코드 리뷰(미확보)
- **이유**: 감리자 수동 검증의 한계. 기계가 잡을 수 있는 것은 기계에게 맡기고, 감리자는 설계 방향 판단에 집중.
- **날짜**: 2026-03-29

## D-007: 엑셀 양식 기반 입력 + 서버 검증
- **결정**: 실무자가 SolarFlow에서 빈 양식 다운로드 → PC에서 입력 → 업로드 시 서버 검증
- **이유**: (1) 엑셀에 드롭다운(제조사, 창고 등) 넣으면 입력 오류 감소 (2) 업로드 시 행별 에러 반환으로 잘못된 데이터 차단
- **날짜**: 2026-03-29

## D-008: receipt_matches.outbound_id FK 지연 추가 [완료]
- **결정**: outbound 테이블이 아직 없으므로 UUID 컬럼만 생성, FK는 Step 9 이후 ALTER TABLE로 추가
- **이유**: 테이블 생성 순서 의존성. receipts/receipt_matches는 Step 8, outbound는 Step 9.
- **상태**: Step 9에서 outbound 테이블 생성 완료. FK 추가 가능 상태.
- **날짜**: 2026-03-29

## D-009: 수금 매칭 합계 검증 Go 허용
- **결정**: receipt_matches의 SUM(matched_amount) ≤ receipt.amount 체크를 Go에서 수행
- **이유**: 두 테이블 조회이지만 단순 합산. Go+Rust 분리 기준의 "한 행 사칙연산" 범위 내.
- **날짜**: 2026-03-29

## D-010: deposit_rate 0~100 범위 검증 추가
- **결정**: 수주의 계약금 비율(deposit_rate)에 0~100 범위 검증 추가
- **이유**: 시공자 자체 판단으로 추가. 계약금 비율이 100%를 넘는 건 실무에서 불가능.
- **날짜**: 2026-03-29

## D-011: limit_changes에 Update/Delete 없음
- **결정**: 한도 변경이력 테이블에 수정/삭제 API를 제공하지 않음 (List, Create만)
- **이유**: 한도 변경은 이력이므로 수정/삭제하면 감사 추적 불가. 잘못 입력 시 새 이력으로 정정.
- **날짜**: 2026-03-29

## D-012: omitempty 일괄 적용
- **결정**: 19개 UpdateRequest 구조체의 모든 포인터 필드에 `json:",omitempty"` 태그 일괄 적용
- **이유**: UpdateRequest의 포인터 필드에 omitempty가 없으면 null 필드도 DB에 전송되어 의도치 않은 덮어쓰기 가능. 부분 업데이트 시 전송하지 않은 필드가 null로 초기화되는 문제 방지.
- **날짜**: 2026-03-29

## D-013: 출고 취소 3단계 (active/cancel_pending/cancelled)
- **결정**: 출고 상태를 active → cancel_pending → cancelled 3단계로 관리
- **이유**: 실무자가 취소 여부를 즉시 결정 못할 수 있음. cancel_pending은 가용재고 미차감. 실무자가 판단 후 삭제 또는 복원.
- **날짜**: 2026-03-29

## D-014: ERP 관리구분과 usage_category 일치
- **결정**: 출고 usage_category를 ERP 관리구분 기반으로 재설계 (기존 7개 → 9개)
- **이유**: ERP 내보내기 시 매핑 정확성. ERP 1,881건 분석 기반으로 재설계. replacement는 construction_damage로 대체. repowering은 outbounds에서 제거, orders.management_category에는 유지.
- **날짜**: 2026-03-29

## D-015: 수주 충당 소스 구분 (fulfillment_source: stock/incoming)
- **결정**: 수주에 fulfillment_source 컬럼 추가 (stock/incoming)
- **이유**: 미착품에도 판매/공사 예약이 걸릴 수 있음. "총확보량"이 실제보다 부풀려지는 것을 방지. 실무자가 "이 수주는 미착품에서 충당"이라는 의사결정을 기록.
- **날짜**: 2026-03-29

## D-016: Axum 선택
- **결정**: Rust 웹 프레임워크로 Axum 채택
- **이유**: SolarFlow 계산엔진은 극한 성능이 아닌 정확한 계산이 목적. Tokio 팀 제작, 타입 안전, 메모리 효율 최고, 학습 곡선 완만. sqlx/serde 등 Tokio 생태계와 자연스럽게 통합.
- **날짜**: 2026-03-29

## D-017: Supabase Session pooler 사용
- **결정**: Supabase Session pooler(포트 5432)를 사용하여 sqlx로 연결
- **이유**: Direct connection은 IPv6 전용이라 fly.io에서 연결 불가. Session pooler(포트 5432)는 IPv4 지원하며 prepared statements도 정상 동작.
- **날짜**: 2026-03-29

## D-018: Cargo.lock 커밋
- **결정**: Cargo.lock을 .gitignore에 넣지 않고 커밋
- **이유**: Rust 공식 권장 — 바이너리 프로젝트는 Cargo.lock을 커밋해야 빌드 재현성(reproducibility) 보장.
- **날짜**: 2026-03-29

## D-019: /health와 /health/ready 분리
- **결정**: 헬스체크를 /health(서버 생존)와 /health/ready(DB 연결)로 분리
- **이유**: /health는 fly.io 헬스체크용 (항상 200, DB 무관). /health/ready는 DB 연결 확인용 (Go에서 Rust 호출 전 상태 확인). DB 장애 시 서버는 살아있지만 계산은 불가능한 상태를 구분.
- **날짜**: 2026-03-29

## D-020: Go↔Rust 통신 패턴
- **결정**: Go에서 Rust가 다운되어도 CRUD 기능 유지 (graceful degradation)
- **이유**: Rust 엔진은 계산 전용이므로, 다운 시 계산 기능만 비활성화. 모든 Rust 호출은 EngineClient.CallCalc()을 통해 일관되게 수행. fly.io auto_stop 시 콜드 스타트 1~3초, 타임아웃 10초로 대응.
- **날짜**: 2026-03-29

## D-021: 재고 집계를 단일 API로 제공
- **결정**: 물리적/가용/미착품을 한 번의 POST /api/calc/inventory 호출로 일괄 계산
- **이유**: 별도 API로 분리하면 시점 차이로 데이터 불일치. 8단계 계산이 서로 의존하므로 한 번에 실행해야 정합성 보장.
- **날짜**: 2026-03-29

## D-022: 장기재고 판별은 최초 입고일 기준 (FIFO 미적용)
- **결정**: 장기재고를 최초 입고일(MIN(actual_arrival)) 기준으로 판별 (normal/warning/critical)
- **이유**: 정확한 FIFO는 건별 추적 필요. 현재는 품번 단위 집계이므로 최초 입고일이 실용적. 실무 문제 시 추후 확장.
- **날짜**: 2026-03-29

## D-023: 부대비용 배분 기준은 capacity_kw 비율
- **결정**: 부대비용을 B/L 라인아이템의 capacity_kw 비율로 배분
- **이유**: 물리적 처리 비용(운송, 보관, 하역)은 용량에 비례. 수량 기준 배분은 Wp이 다른 모듈 혼합 시 부정확.
- **날짜**: 2026-03-29

## D-024: 현재 환율은 최근 면장 환율 사용 (실시간 API 미연동)
- **결정**: 환율 비교 시 가장 최근 면장의 exchange_rate를 현재 환율로 사용
- **이유**: 외부 의존성 최소화. 실시간 환율 필요 시 Phase 확장.
- **날짜**: 2026-03-29

## D-025: Landed Cost 계산 결과는 save=true일 때만 DB 저장
- **결정**: 계산 결과를 save=false(기본값)로 미리보기, save=true일 때만 cost_details 업데이트
- **이유**: Rust의 직접 WRITE는 Go CRUD 통제권과 충돌. 미리보기 후 확인하고 저장하는 것이 실무에 자연스러움.
- **날짜**: 2026-03-29

## D-026: allocated_expenses를 동적 맵으로 처리
- **결정**: 부대비용 배분 내역을 HashMap<String, f64> / map[string]float64로 처리 (키 하드코딩 없음)
- **이유**: expense_type이 추가되어도 코드 변경 불필요. 확장성.
- **날짜**: 2026-03-29

## D-027: LC 수수료 환율은 B/L 환율 우선, 없으면 최근 면장 환율
- **결정**: LC 수수료 계산 시 환율을 B/L 환율 → 최근 면장 환율 순으로 조회
- **이유**: LC에 연결된 B/L이 있으면 해당 B/L의 환율이 가장 정확. 없으면 최근 면장 환율로 대체.
- **날짜**: 2026-03-29

## D-028: 한도 복원 타임라인은 maturity_date 기준
- **결정**: LC 한도 복원을 settlement_date가 아닌 maturity_date 기준으로 계산
- **이유**: settlement_date는 실제 결제 후에만 기록됨. 계획 단계에서는 maturity_date가 유일한 기준.
- **날짜**: 2026-03-29

## D-029: 만기 알림 severity (0~3일 critical, 4~7일 warning)
- **결정**: LC 만기 알림을 0~3일 critical, 4~7일 warning으로 분류
- **이유**: 3일 이내는 즉시 조치 필요, 4~7일은 사전 준비 구간.
- **날짜**: 2026-03-29

## D-030: LC 수수료는 자동 계산(예상). 수동 입력은 Phase 확장.
- **결정**: 현재는 요율 기반 자동 계산만. 실제 은행 청구 금액 수동 입력은 Phase 확장 (LC 수수료 자동 계산은 완료, 수동 보정만 미구현).
- **이유**: 1원 단위 차이는 마진에 영향 없으나 정확성 위해 수동 보정 기능 예정.
- **날짜**: 2026-03-29

## D-031: 마진 원가는 품번별 가중평균 (FIFO 미적용)
- **결정**: 마진 분석 시 원가를 품번별 가중평균(입고 수량 기준)으로 계산
- **이유**: FIFO는 건별 추적 필요. 가중평균이 실무에서 충분하고 구현 간결.
- **날짜**: 2026-03-29

## D-032: cost_basis 선택 (cif/landed)
- **결정**: 마진 분석에서 원가 기준을 cif(회계용)/landed(실무용) 중 선택 가능
- **이유**: 회계팀은 CIF 기준 보고, 판매팀은 부대비용 포함 Landed 기준 의사결정. 한 API에서 둘 다 지원.
- **날짜**: 2026-03-29

## D-033: 미수금 경과일 기준은 출고일 (outbound_date)
- **결정**: 미수금 경과일을 세금계산서 발행일이 아닌 출고일 기준으로 계산
- **이유**: 세금계산서 미발행 건도 포함해야 미수금 전체 파악 가능. 출고일이 더 보수적.
- **날짜**: 2026-03-29

## D-034: 거래처 분석에 마진율+계약금비율 포함
- **결정**: 거래처별 분석에 avg_margin_rate, avg_deposit_rate 포함
- **이유**: "미수금 많은 업체 → 마진은? → 계약금은?" 한 화면에서 판단 가능.
- **날짜**: 2026-03-29

## D-035: 수급 전망 입고예정은 B/L ETA 기준
- **결정**: 입고예정을 PO 계약일이 아닌 B/L ETA 기준으로 월별 배분
- **이유**: PO 계약일은 실제 입고와 수개월 차이. B/L 미생성분은 unscheduled로 분리.
- **날짜**: 2026-03-29

## D-036: 수급 전망 출고예정은 delivery_due 기준
- **결정**: 출고예정을 delivery_due(납기 요청일) 기준으로 월별 배분
- **이유**: 납기 요청일이 실무 출고 시점. NULL은 unscheduled로 분리.
- **날짜**: 2026-03-29

## D-037: fulfillment_source='incoming' 수주는 수급 전망에서 제외
- **결정**: 미착품 충당 수주는 월별 재고 흐름 계산에서 제외
- **이유**: 미착품 충당 수주는 물리적 재고에 영향 안 줌. 포함하면 이중 차감.
- **날짜**: 2026-03-29

## D-038: closing 음수 허용 (insufficient 플래그)
- **결정**: 기말재고가 음수가 되어도 0으로 자르지 않고 insufficient=true 표시
- **이유**: 실무자 사전 경고. 0으로 자르면 이후 달 예측 부정확.
- **날짜**: 2026-03-29

## D-039: 수금 매칭 추천은 미수금 합계가 입금액을 초과하지 않는 조합만 제시
- **결정**: 과매칭 방지 — 조합 합계 > 입금액인 경우 제외
- **이유**: 과매칭은 실무 발생 안 함. 차액은 선수금/이월 (Phase 4).
- **날짜**: 2026-03-29

## D-040: 부분집합 탐색 N<=20 완전 탐색, N>20 greedy
- **결정**: 미수금 N<=20이면 비트마스크 완전 탐색(2^20 약 100만), N>20이면 greedy
- **이유**: N=20이면 1초 이내. 20건 초과는 극히 드묾. greedy는 날짜 오래된 순으로 실무 관행과 일치.
- **날짜**: 2026-03-29

## D-041: 미수금 status 기준 Step 16과 동일 (30/60일)
- **결정**: 미수금 목록의 status를 거래처 분석과 동일 기준(30/60일)으로 적용
- **이유**: 거래처 분석과 미수금 목록의 status 일관성 유지.
- **날짜**: 2026-03-29

## D-042: 수금 매칭은 현재 outbound_id 기준
- **결정**: 현재 매칭 단위는 outbound_id. Phase 확장 (현재 outbound 기준으로 정상 동작).
- **이유**: 현재 알고리즘은 기준 단위와 무관하게 동일 동작. 실데이터 넣으면서 스키마 개선이 현실적.
- **날짜**: 2026-03-29

## D-043: 제조사 별칭 HashMap 하드코딩. 거래처 별칭은 그룹 개념.
- **결정**: 별칭은 코드 내 HashMap. 거래처는 "그룹"(신명=신명+미래+에스엠), 검색 시 원래 키워드+별칭 둘 다 ILIKE.
- **이유**: Phase 확장 시 DB 테이블로. 현재 제조사 15개, 거래처 그룹 2개로 하드코딩 충분.
- **날짜**: 2026-03-29

## D-044: 키워드 패턴 매칭 (LLM 미사용)
- **결정**: 의도 분류는 키워드 매칭. LLM 미사용.
- **이유**: 실무 패턴 제한적(7가지). Phase 확장 시 LLM.
- **날짜**: 2026-03-29

## D-045: fallback은 ILIKE 전체 텍스트
- **결정**: 의도 파악 불가 시 ILIKE 검색 (product, partner, site)
- **이유**: 한국어 tsvector 제한. 데이터 증가 시 pg_trgm.
- **날짜**: 2026-03-29

## D-046: 검색 결과에 link 포함
- **결정**: 각 결과에 SearchLink(module, params) 포함
- **이유**: Phase 4 프론트에서 "결과 클릭 → 상세 이동" 직접 사용.
- **날짜**: 2026-03-29

## D-047: AIKO와 에스디엔은 별도 제조사
- **결정**: AIKO(아이코)와 에스디엔(SDN)은 별도 제조사로 관리
- **이유**: 감리 확정. 실무에서 별도 거래.
- **날짜**: 2026-03-29

## D-048: harness/ 폴더 분리
- **결정**: 하네스 파일(RULES, AGENTS, DECISIONS, PROGRESS, 설계문서, CHECKLIST)을 harness/ 폴더로 분리
- **이유**: 하네스와 코드 분리. Go/Rust 공통 규칙 참조 용이. TASK 파일은 backend/tasks/로 분리.
- **날짜**: 2026-03-29

## D-049: CLAUDE.md 도입
- **결정**: 프로젝트 루트에 CLAUDE.md를 배치하여 Claude Code 새 세션에서 자동 읽기 순서 지정
- **이유**: Claude Code는 프로젝트 루트의 CLAUDE.md를 자동으로 읽음. 읽기 순서를 명시하여 매번 "먼저 이것을 읽어주세요" 지시 불필요.
- **날짜**: 2026-03-29

## D-050: PROGRESS.md 영구적 컨텍스트 앵커
- **결정**: PROGRESS.md를 "이것만 읽으면 현재 위치 즉시 파악" 가능한 영구적 앵커로 강화
- **이유**: 새 대화창, 새 세션에서 프로젝트 상태를 파악하는 데 걸리는 시간 최소화. 현재 상태 요약 + Phase 이력 + 미해결 사항 + API 목록을 한 파일에 집약.
- **날짜**: 2026-03-29

## D-051: 프론트→Go→Rust 호출 체인
- **결정**: 프론트엔드가 Rust 엔진을 직접 호출하지 않고, 반드시 Go를 경유
- **이유**: (1) 인증 우회 방지 — Go AuthMiddleware를 거쳐야만 접근 가능 (2) CORS 1곳(Go)에서 관리 (3) Rust 다운 시 Go에서 graceful degradation(503 + engine_status) 처리
- **날짜**: 2026-03-29

## D-052: shadcn/ui 선택
- **결정**: UI 프레임워크로 shadcn/ui 채택 (base-nova 스타일)
- **이유**: 소스 복사 방식으로 완전 커스텀 가능. Radix UI 기반 접근성. 2026년 React 생태계 표준.
- **날짜**: 2026-03-29

## D-053: Zustand 상태 관리
- **결정**: 전역 상태 관리에 Zustand 사용
- **이유**: 전역 상태가 인증 + 법인 선택 정도로 가벼움. Redux는 이 규모에 과잉. Zustand는 보일러플레이트 최소.
- **날짜**: 2026-03-29

## D-054: CalcProxy 패턴 — 바이트 그대로 전달
- **결정**: Go CalcProxy가 프론트 요청 바이트를 해석하지 않고 Rust에 그대로 중계
- **이유**: Go가 Rust 요청/응답 구조를 알 필요 없음. Rust API 변경 시 Go 수정 불필요. 기존 타입 안전 메서드(GetInventory, CalcLandedCost 등)는 Go 내부 호출용으로 유지.
- **날짜**: 2026-03-29

## D-055: user_profiles 컬럼명은 실제 DB 기준
- **결정**: user_profiles 테이블의 PK는 `id` (user_id 아님), 이름은 `full_name` (name 아님). `allowed_modules`, `company_id`는 현재 DB에 없으므로 구조체에서 제거. `department`, `phone`, `avatar_url`을 nullable로 추가.
- **이유**: Phase 1에서 생성된 실제 테이블 스키마를 존중. 설계문서와 다르지만 DB를 정본으로 삼음. `allowed_modules`, `company_id`는 Phase 확장 시 ALTER TABLE로 추가.
- **날짜**: 2026-03-29

## D-056: 사이드바 재고 최상단 배치
- **결정**: 사이드바 메뉴에서 재고 현황을 최상단에 배치 (대시보드보다 위)
- **이유**: 실무에서 가장 자주 보는 화면. 재고 확인이 업무 시작점.
- **날짜**: 2026-03-29

## D-057: 마스터 CRUD는 Dialog 기반
- **결정**: 마스터 데이터 생성/수정을 별도 페이지가 아닌 Dialog(모달) 안에서 처리
- **이유**: 마스터 데이터 필드가 적어 별도 페이지 불필요. 목록에서 바로 생성/수정 가능.
- **날짜**: 2026-03-29

## D-058: 프론트 정렬/페이지네이션
- **결정**: 마스터 목록의 정렬과 페이지네이션을 서버가 아닌 프론트에서 처리
- **이유**: 마스터 데이터 수백 건 이하. 서버 왕복 없이 즉시 응답.
- **날짜**: 2026-03-29

## D-059: 프론트 타입은 snake_case
- **결정**: Go API 응답의 snake_case JSON 필드명을 프론트 타입에서도 그대로 사용 (camelCase 변환 안 함)
- **이유**: Go API 응답과 일치시켜 매핑 실수 방지. 변환 레이어 불필요.
- **날짜**: 2026-03-29

## D-060: 즉시 수정 원칙 도입
- **결정**: 문제가 확인되면 바로 조치. 미루기 금지. 예외 시 DECISIONS.md에 이유 기록 필수.
- **이유**: "나중에 하자"가 누적되면 기술 부채가 가속. Phase 3까지 미해결 5건이 쌓인 경험에서 도출. 미룸이 정당한 경우(DB 마이그레이션 필요, 다른 Step 의존 등)만 허용하되 반드시 기록.
- **날짜**: 2026-03-29

## D-061: PO 입고현황은 프론트에서 B/L 수량 합산
- **결정**: PO 상세의 입고현황 탭에서 B/L 목록을 GET으로 가져와 프론트에서 수량 합산 (계약량/LC개설량/선적완료/입고완료/잔여량)
- **이유**: PO당 B/L 수십 건 이하로 성능 문제 없음. 별도 집계 API를 만들면 유지보수 대상 증가. 데이터 증가 시 Rust API로 이동.
- **날짜**: 2026-03-30

## D-062: LC 한도 수요 예측 기능
- **결정**: 모든 해외 PO는 T/T 5~10% + 잔액 LC 구조. LC 개설 시점: PO 계약 후 30일 내. 3개월 전부터 매월 경고.
- **이유**: Alex 요청. LC 한도 부족 시 선적 지연/계약 차질 발생. 사전 예측으로 한도 증액/선적 조정/TT 비율 상향 등 대응 가능.
- **구현**: 프론트에서 Go API 조합 (PO+TT+LC+Rust lc-limit-timeline). D-061 패턴. Phase 확장 시 Rust로 이동.
- **날짜**: 2026-04-01

## D-063: 엑셀 업로드 미리보기→확정 2단계
- **결정**: 엑셀 업로드는 D-025 Landed Cost와 동일한 미리보기→확정 2단계 패턴. 중복 등록 방지. 프론트에서 기본 검증(필수/코드존재/양수/허용값/날짜) 후 미리보기 표시, 사용자 확인 후 확정 시 Go API 호출.
- **이유**: (1) 엑셀 대량 등록은 되돌리기 어려움 — 미리보기에서 에러 사전 차단 (2) D-025에서 검증된 패턴 재활용으로 UX 일관성 확보 (3) 확정 버튼은 Step 29B에서 활성화 — 프론트 검증 로직만 먼저 완성.
- **날짜**: 2026-04-01

## D-064: PDF 자동 데이터 입력 (Phase 5 예정)
- **결정**: Phase 4에서는 엑셀 Import/Export로 진행. PDF 자동 입력(면장/C.I/발주서 등)은 Phase 5에서 구현.
- **이유**: (1) 양식이 제조사마다 다름(진코/트리나/라이젠 각각 다른 C/I 포맷) (2) 잘못된 파싱이 원가 계산에 치명적(수억 원 단위) (3) Claude API 연동 시 프론트 PDF변환+Go API프록시+키관리+파싱검증 레이어 추가로 복잡도 상승 (4) 엑셀 Import 양식 확정 후 "어디에 넣을지"가 명확해짐 (5) 실무 사용 후 가장 빈번한 서류 확인 뒤 우선순위 결정.
- **후보 기술**: Claude API(양식 불문) 또는 정형 PDF 좌표 파싱.
- **날짜**: 2026-04-01

## D-065: Import API map[string]interface{} 사용 허용 (RULES.md 예외)
- **결정**: Import API의 map[string]interface{} 사용 허용. 엑셀 7종 동적 필드 처리 위해 불가피.
- **범위**: handler/import.go, model/import.go에 한정. 다른 핸들러에서는 금지 유지.
- **이유**: (1) 7종 Import가 각각 다른 필드셋을 가짐 (2) 프론트에서 엑셀 파싱 결과를 그대로 전송 (3) map에서 꺼내 기존 Create*Request 구조체로 변환 후 Validate() 실행하므로 타입 안전성 확보 (4) 린터 스크립트에서 이 2개 파일 예외 등록 완료.
- **날짜**: 2026-04-02

## D-066: 매출 Import outbound_id 직접 매칭 (감리 지적 1)
- **결정**: 매출 양식에서 출고일/품번코드 참조 방식을 outbound_id 필수로 변경.
- **이유**: 출고일+품번만으로는 같은 품번의 여러 출고를 구분할 수 없음. outbound_id를 직접 지정하면 1:1 매칭이 보장됨. 코드표에 outbound 목록(ID+출고일+수량+현장명) 제공.
- **날짜**: 2026-04-02

## D-067: 아마란스 매출마감 내보내기 Step 32로 연기
- **결정**: 아마란스 매출마감(매출전표) 내보내기는 Step 32에서 구현.
- **이유**: 실물 양식 미확보. 입고/출고 양식은 아마란스 헬프에서 확인 완료되었으나 매출마감은 실무팀에서 별도 확인 필요.
- **날짜**: 2026-04-02

## D-068: 아마란스 관리구분(MGMT_CD) 빈값 내보내기
- **결정**: usage_category → 아마란스 관리구분 코드 매핑을 현재 모르므로 빈값으로 내보내기. 실무자가 ERP에서 수동 입력. Phase 확장 시 매핑 테이블 추가.
- **날짜**: 2026-04-02

## D-069: ES256 JWKS 인증 지원
- **결정**: Supabase JWT가 ES256(ECDSA)인데 Go AuthMiddleware가 HMAC만 허용하여 모든 로그인 401 거부됨. keyfunc/v2 라이브러리로 JWKS 엔드포인트에서 공개키 자동 로드(1시간 갱신). ES256 우선, HMAC 폴백 유지.
- **이유**: Supabase가 ES256으로 JWT 서명. HMAC 전용 검증으로는 모든 토큰이 signature invalid. JWKS는 키 로테이션에도 자동 대응.
- **범위**: middleware/auth.go — getJWKS() lazy init, jwt.Parse에서 SigningMethodECDSA/SigningMethodHMAC 분기.
- **환경변수**: SUPABASE_URL에서 JWKS URL 자동 유도 ({URL}/auth/v1/.well-known/jwks.json). 또는 SUPABASE_JWKS_URL 직접 지정.
- **날짜**: 2026-04-03

## D-070: 배포 시 RLS 비활성화 (Phase 확장 시 service_role key 전환)
- **결정**: 전체 22개 테이블 RLS DISABLE. Go가 anon key로 Supabase 접속하므로 RLS 활성 시 빈 결과 반환됨.
- **이유**: (1) RLS 정책이 auth.uid() 기준인데, Go 서버는 서비스 역할로 접근해야 함 (2) Go AuthMiddleware에서 이미 JWT 검증+역할 확인을 수행하므로 행 수준 보안은 이중 (3) Phase 확장 시 service_role key로 전환하면 RLS 재활성화 가능.
- **대상**: companies, manufacturers, products, partners, warehouses, banks, purchase_orders, po_line_items, lc_records, tt_remittances, bl_shipments, bl_line_items, outbounds, sales, orders, receipts, receipt_matches, declarations, declaration_costs, expenses, price_histories, bank_limit_changes, notes, user_profiles.
- **날짜**: 2026-04-03

## D-071: 전체 법인(all) 선택 시 법인별 호출 합산 방식
- **결정**: "전체(all)" 법인 선택 시 Rust Calc API는 법인 목록 조회 → Promise.all 병렬 호출 → 결과 merge. CRUD API는 company_id 파라미터 생략으로 전체 반환.
- **이유**: (1) Rust Calc API는 company_id 필수 — "all"을 직접 보내면 에러 (2) 법인별 호출 후 합산이 가장 정확 (3) CRUD API는 Go에서 company_id 쿼리 없으면 전체 반환하도록 이미 구현됨.
- **범위**: lib/companyUtils.ts (fetchCalc, companyParams, companyQueryUrl), useDashboard, useAlerts, useInventory, useBanking, useForecast, useLCDemand, useSearch + CRUD hooks 10개.
- **법인 캐시**: 5분 TTL. getActiveCompanyIds()에서 GET /api/v1/companies 호출 결과 캐시.
- **날짜**: 2026-04-03

## D-072: AWS Lightsail 서울 리전 이전
- **결정**: Fly.io 도쿄 → AWS Lightsail 서울 리전으로 Go 백엔드 + Rust 엔진 이전. 직접 바이너리 + systemd 서비스. Docker 미사용.
- **이유**: (1) Fly.io 도쿄 리전은 한국에서 RTT 50-80ms, Lightsail 서울은 2-5ms (2) 직접 바이너리가 Docker 대비 메모리/디스크 절약 (3) systemd가 프로세스 재시작/로그 관리를 안정적으로 처리 (4) Lightsail 서울 $5/월 인스턴스로 충분.
- **날짜**: 2026-04-04

## D-073: 도메인 solarflow3.com 설정
- **결정**: api.solarflow3.com = Go 백엔드 + Rust 엔진 (Lightsail), app.solarflow3.com = 프론트엔드 (Cloudflare Pages).
- **이유**: (1) 서브도메인으로 역할 분리 (2) api.는 Caddy→Go(:8080)→Rust(:8081) 내부 통신 (3) app.는 Cloudflare Pages CNAME — CDN 캐싱 + DDoS 보호.
- **날짜**: 2026-04-04

## D-074: Caddy 리버스 프록시 + 자동 SSL
- **결정**: Caddy를 api.solarflow3.com의 리버스 프록시로 사용. HTTPS 자동 발급/갱신.
- **이유**: (1) Let's Encrypt 인증서를 자동 발급/갱신 — 수동 관리 불필요 (2) 설정 파일 2-3줄로 리버스 프록시 완성 (3) Nginx 대비 설정 복잡도 1/10 (4) HTTP/2, OCSP stapling 기본 지원.
- **날짜**: 2026-04-04

## D-075: PostgREST 로컬 도입
- **결정**: Go 백엔드가 supabase-go(PostgREST HTTP 방식)를 사용하므로, 로컬 PostgreSQL 앞에 PostgREST를 배치. Go 코드 변경 0줄.
- **이유**: Go 핸들러 30개+ 전면 수정(pgx 전환)은 리스크가 높고, PostgREST 로컬 설치가 코드 변경 없이 동일 효과.
- **날짜**: 2026-04-05

## D-076: Caddy 경로 변환 (server-root-path 대체)
- **결정**: PostgREST의 server-root-path="/rest/v1"이 예상대로 동작하지 않아, Caddy로 /rest/v1/* prefix를 strip한 후 PostgREST로 전달.
- **이유**: 실측 테스트에서 확인. PostgREST는 /companies로 서빙하지만 supabase-go는 /rest/v1/companies로 요청.
- **날짜**: 2026-04-05

## D-077: auto-provision (신규 사용자 자동 프로필 생성)
- **결정**: Go 인증 미들웨어 + /users/me 핸들러에서 JWT user_id로 user_profiles 조회, 없으면 자동 INSERT. 기본 role: viewer.
- **이유**: Supabase Auth에서 계정 생성 + 로컬 DB 수동 추가 = 두 곳 작업 필요. auto-provision으로 계정 생성만 하면 첫 로그인 시 자동 등록.
- **날짜**: 2026-04-05

## D-078: Tailscale 외부 접속
- **결정**: 도메인/SSL 없이 Tailscale VPN으로 외부 접속. Mac mini IP 100.123.70.19:5173.
- **이유**: 비용 $0, 설치 간편, 5-10명 규모에 충분. 도메인 불필요.
- **날짜**: 2026-04-05

## D-079: 프론트엔드 정적 서빙 (Caddy)
- **결정**: npm run dev 대신 Caddy가 빌드된 dist/ 폴더를 직접 서빙. /api/* 요청은 Go로 프록시.
- **이유**: 터미널 수동 실행 불필요. launchd 자동 시작과 결합하여 재부팅 후 즉시 서비스 가능.
- **날짜**: 2026-04-05

## D-082: 결제조건 정본은 PO, B/L은 보조
- **결정**: 결제조건/인코텀즈의 정본(Source of Truth)은 PO. B/L 상의 payment_terms는 PO가 없는 과거 데이터/국내직접구매 보조 용도로만 사용.
- **이유**: PO 1:N B/L 관계에서 동일한 계약 조건이 여러 B/L에 반복 복제되면 불일치 위험. PO에서 상속받는 단일 정본 정책으로 통일.
- **날짜**: 2026-04-07

## D-083: 입고완료는 DB에 completed 저장, 화면 표시는 유형별로 분기
- **결정**: 모든 입고유형(해외직수입/국내구매/그룹내구매)의 "입고완료" 상태는 DB에 `status='completed'`로 저장. 화면 표시만 유형에 따라 다르게 (해외직수입: "완료", 국내/그룹: "입고완료"). 국내/그룹은 scheduled→completed 2단계만 사용("입고중"→"입고완료"로 표시).
- **재고 반영 기준**: `shipping/arrived/customs` → 미착품, `completed/erp_done` → 물리적 재고. Rust `engine/src/calc/inventory.rs`의 `fetch_bl_incoming`이 BL 상태 직접 기반으로 집계(PO 매칭 미사용, 유형 무관).
- **이유**: DB 스키마를 단일 status enum으로 유지하여 재고 집계 SQL을 단순화. 화면 표시만 분기해서 UX 혼동 제거.
- **날짜**: 2026-04-07

## D-084: 사이드바 메뉴 3그룹 — 업무흐름/현황분석/도구
- **결정**: 사이드바 메뉴를 업무 수행 순서대로 3그룹으로 재편.
  1. **업무 흐름**: 발주/결제 → 입고관리 → 면장/원가 → 수주/수금 → 출고/판매
  2. **현황/분석**: 재고현황, 은행/LC, 대시보드
  3. **도구**: 마스터관리, 검색, 메모, 결재안, 설정
- **이유**: 기존 "운영/기타" 2그룹 구조가 실제 업무 흐름(좌→우 진행)과 일치하지 않아 신규 사용자 학습 비용이 높았음. 업무 순서대로 나열하여 "다음 단계는 무엇"이 직관적으로 보이도록 재배치.
- **날짜**: 2026-04-07

## D-085: PO → 입고 → 면장 데이터 연결 + 독립 입력 경로 병존
- **결정**: 업무 흐름 간 데이터 이동(자동채움)을 표준화하되 독립 입력 경로도 유지.
  - **PO → 입고**: PO 상세에 "입고 등록" 버튼. 클릭 시 `/inbound?po=xxx`로 이동 → 입고 폼이 열리면서 PO의 법인/제조사/인코텀즈/결제조건/라인 품번·단가 자동 채움. 수량은 비움(분할선적 대비). 모든 필드 수정 가능.
  - **입고 → 면장**: 입고 상세에 "면장 등록" 버튼(해외직수입만). `/customs?bl=xxx`로 이동 → 면장 폼이 B/L 자동 선택 상태로 열림. B/L:면장 = 1:1 원칙. 향후 분할통관 대비 수량 수정 가능.
  - **독립 입력**: 입고관리 "새로 등록"은 기존대로 유지. 입고 폼 최상단 "P/O 연결 (선택사항)" 드롭다운으로 PO 매칭/미매칭 선택 가능. 과거 데이터, 긴급 입고용.
  - **단가 수정 가능**: PO 단가는 기본값일 뿐이며 실무자가 자유롭게 수정. B/L 라인의 실단가가 원가 산정의 정본.
- **이유**: 실무 흐름(PO 작성 → 선적 → 입고 → 통관)이 선형적이지만 과거 데이터 이관/긴급건도 자주 발생. 자동채움으로 일상 업무 생산성을 높이되, 독립 입력 경로를 막지 않아 예외 상황에도 대응 가능하도록 병존.
- **날짜**: 2026-04-07

## D-086: PO 필수 연결 + 분할선적 + 환율 3단계 비교
- **결정**: 모든 입고는 PO에 연결 필수 (가상 PO라도 생성). 분할선적은 동일 PO에 BL 여러 건. 환율은 PO(발주) / 면장(통관) / TT·LC(송금) 3단계 각각 기록하여 비교 가능하게 함. 입고등록 시 PO 선택 → 제조사/품목/단가 자동 채움.
- **이유**: BL번호가 공식 추적키, PO번호는 내부 그룹핑 도구. 기존 데이터는 가상 PO로 묶음. 환율 3단계 비교로 환율 손익 가시화.
- **날짜**: 2026-04-08

## D-087: 권한 관리 — 역할별 메뉴/기능 접근 설정 화면
- **결정**: admin이 설정 화면에서 역할(5종)별로 메뉴·민감정보 접근을 체크박스로 직접 설정. DB에 저장하여 런타임에 적용.
- **구조**:
  - DB 테이블: `permission_settings (menu_key, role, allowed boolean)`
  - 메뉴 키: procurement, lc, inbound, inventory, orders, outbound, receipts, dashboard, banking, customs, masters, search, ocr, memo, approval
  - 민감정보 키: feature:show_price (단가), feature:show_margin (이익률/이익액), feature:show_full_dashboard
  - Go API: GET/PUT /api/v1/settings/permissions
  - 프론트: 설정 화면에서 역할×메뉴 매트릭스 테이블, 체크박스로 토글
  - Sidebar의 `canSee()`를 DB 기반 동적 권한으로 교체
  - 민감 컴포넌트에 `usePermission('feature:show_price')` 훅 적용
- **이유**: 하드코딩된 roles 배열(현재 Sidebar.tsx)은 사람이 늘어날수록 코드 수정이 필요하여 비현실적. 운영자가 UI에서 직접 조정 가능한 구조가 지속가능.
- **초기 기본값**: admin=전체허용, executive=민감정보 허용+입력불가, manager=입력가능+민감정보불가, staff=입력가능+민감정보불가, viewer=지정메뉴만+민감정보불가
- **날짜**: 2026-04-13

## D-088: 개발 방식 — 대화 기반 점진적 개선
- **결정**: 설계 완료 후 대규모 일괄 개발 없이, 메뉴별·기능별로 대화를 통해 점진적으로 수정·추가.
- **이유**: (1) 실제 운영 데이터가 있어 즉시 검증 가능 (2) 사용자가 실제로 써보면서 피드백이 발생하므로 선설계보다 유리 (3) Go+Rust+React 분리 구조가 명확해 영향 범위 예측 용이
- **유의사항**: 여러 화면에 걸친 변경은 영향 범위를 먼저 확인 후 진행. 중요 판단은 DECISIONS.md에 기록.
- **날짜**: 2026-04-13

## D-089: 운영 이관 데이터와 과거 참고자료 분리
- **결정**: 운영 전환 시 SolarFlow 실제 업무 테이블에는 전환일 이후 재고, 미착, 수주, 출고, 매출, 수금에 영향을 주는 데이터만 입력한다. 2025-01~2026-03 과거 매출/이익/구매/판매/공사사용 자료는 별도 legacy 영역 또는 참고 화면으로 분리한다.
- **이유**: 과거 자료를 활성 입고/출고/매출 테이블에 섞으면 가용재고, 수급전망, 대시보드, 이익률 분석이 현재 운영 상태와 맞지 않게 된다. 기초재고와 미착품은 운영에 영향을 주므로 이관하되, 과거 실적은 참고 분석으로만 사용한다.
- **운영 기준**: 실제 이관 데이터는 아마란스와 맞춘 입고/출고/매출 엑셀 양식으로 업로드한다. PO/LC/B/L이 없는 과거 진행 건은 `MIG-*` 임의 번호를 부여해 추적키를 만든다.
- **날짜**: 2026-04-26

## D-090: LC도 PO 라인아이템 단위로 추적
- **결정**: PO가 여러 품목을 가질 수 있으므로 LC도 `lc_line_items` 하위 테이블을 두고 `po_line_id` 기준으로 품목별 수량, 용량, 금액을 저장한다. LC 본문(`lc_records`)은 합계 금액/MW/수량만 유지한다.
- **이유**: 기존 LC 합계만으로는 다품목 PO에서 어떤 제조사·규격·품번이 LC에 포함되었는지 추적할 수 없다. B/L 입고가 PO 라인을 끌고 오는 구조와 맞춰 PO → LC → B/L 흐름을 품목 단위로 연결한다.
- **날짜**: 2026-04-27

## D-091: Supabase Auth JWT와 로컬 PostgREST JWT 분리
- **결정**: 로컬 PostgREST의 `jwt-secret`은 `SUPABASE_JWT_SECRET`이 아니라 `POSTGREST_JWT_SECRET`을 사용한다. Go 백엔드가 로컬 PostgREST에 접근할 때 쓰는 `SUPABASE_KEY`는 새 `POSTGREST_JWT_SECRET`으로 서명한 로컬 전용 JWT로 교체한다. Supabase Auth 검증용 `SUPABASE_JWT_SECRET`은 로그인 토큰 HMAC 폴백 용도로만 유지한다.
- **이유**: 공개 GitHub 히스토리에 과거 환경값이 노출되었을 가능성이 있으므로, 로컬 DB API(PostgREST) 접근키를 Supabase 프로젝트 JWT secret과 분리해야 한다. 분리 후에는 로컬 PostgREST 키 회전이 Supabase 로그인/Auth 설정에 영향을 주지 않는다.
- **운영 기준**: `postgrest.conf`에는 비밀값을 직접 쓰지 않고 `$(POSTGREST_JWT_SECRET)`만 둔다. 실제 값은 launchd plist와 로컬 `.env*`에만 둔다.
- **날짜**: 2026-04-27

## D-092: 개발 반복 검증과 서비스 반영 스크립트 표준화
- **결정**: 루트 `scripts/`에 전체 검증(`verify_all.sh`)과 서비스 반영(`apply_go.sh`, `apply_rust.sh`, `apply_frontend.sh`) 스크립트를 둔다. 기본 전체 검증은 Go/Rust/프론트 빌드·테스트를 차단 기준으로 삼고, 기존 RULES lint 부채는 advisory로 표시한다. 엄격 차단이 필요할 때는 `STRICT_RULES=1 ./scripts/verify_all.sh`를 사용한다.
- **이유**: 매 작업마다 명령어를 새로 조합하면 누락과 순서 실수가 반복된다. 특히 Go 변경 후 코드서명과 launchd bootout/bootstrap, Request 구조체 변경 후 스키마 검사가 빠지면 운영 화면에서 500 회귀가 난다. 반복 절차를 스크립트로 고정해 개발 속도를 높이고, 기존 lint 부채는 별도 정리 전까지 일상 검증을 막지 않도록 분리한다.
- **날짜**: 2026-04-28

## D-093: 변경 파일 기준 선택 검증
- **결정**: 전체 검증과 별도로 `scripts/verify_changed.sh`를 둔다. 변경 파일 경로를 기준으로 backend, engine, frontend, shell syntax 검증만 선택 실행하고, 알 수 없는 경로가 바뀌면 `verify_all.sh`로 자동 전환한다.
- **이유**: 모든 작업마다 Go/Rust/프론트 전체 검증을 돌리면 UI 단순 변경이나 문서 변경에서도 시간이 불필요하게 든다. 변경 범위에 맞는 검증을 먼저 돌리고, 릴리즈/PR 전에는 전체 검증을 쓰는 2단 구조가 개발 속도와 안정성의 균형이 좋다.
- **날짜**: 2026-04-28

## D-094: 핸들러 다단계 변경은 PostgREST RPC 트랜잭션으로 처리
- **결정**: LC 본문+라인 저장, 출고 생성/수정/삭제, PO 삭제, 면장 삭제처럼 여러 테이블을 함께 변경하는 핸들러는 Go에서 순차 호출하지 않고 PostgreSQL 함수 1회 호출로 묶는다.
- **이유**: Go에서 여러 PostgREST 요청을 순서대로 보내면 중간 실패 시 일부 테이블만 변경될 수 있다. PostgreSQL 함수는 단일 문장 트랜잭션으로 실행되어 실패 시 전체 롤백되므로 생산 데이터 정합성 위험을 줄인다.
- **범위**: `sf_create_lc_with_lines`, `sf_update_lc_with_lines`, `sf_create_outbound`, `sf_update_outbound`, `sf_delete_outbound`, `sf_delete_purchase_order`, `sf_delete_declaration`, `sf_recalculate_order_progress`.
- **운영 기준**: 함수 추가/변경 후 PostgREST 스키마 캐시를 갱신해야 한다. 출고 등록/수정은 저장 전에 Rust 재고 집계 결과로 active 출고의 가용재고를 검증한다.
- **날짜**: 2026-04-28

## D-095: 운영 데이터 감사 로그 + soft cancel
- **결정**: PO, LC, 출고, 매출은 `audit_logs`에 create/update/delete 요청자와 변경 전후 JSON을 남긴다. DELETE API는 실제 삭제 대신 업무 테이블의 상태를 `cancelled`로 바꾸는 soft cancel로 처리한다.
- **이유**: 운영 데이터는 재고, 미착, 한도, 매출, 수금 분석에 연결되므로 실제 삭제 시 “누가 무엇을 지웠는지”와 연결 이력을 잃는다. 취소 상태로 보존하면 실무 오류 정정과 감사 추적이 가능하다.
- **운영 기준**: `audit_logs.action='delete'`는 API 삭제 요청을 뜻한다. 실제 업무 행은 삭제되지 않고 `purchase_orders.status='cancelled'`, `lc_records.status='cancelled'`, `outbounds.status='cancelled'`, `sales.status='cancelled'`로 남긴다. 계산/분석 쿼리는 취소 매출을 기본 집계에서 제외한다.
- **날짜**: 2026-04-28

## D-096: OCR은 자동 저장 전 미리보기 워크벤치로 내장
- **결정**: `../module` 프로젝트의 PaddleOCR/RapidOCR ONNX sidecar 방식을 SolarFlow에 내장하되, OCR 결과를 PO/LC/B/L/면장 등에 즉시 저장하지 않고 `/ocr` 화면의 원문 검토 워크벤치로 먼저 제공한다.
- **이유**: C/I, 면장, 규격서 양식이 제조사·기관마다 다르고, 잘못 읽은 단가·수량·환율은 원가와 재고에 직접 영향을 준다. 따라서 자동 등록보다 "추출 → 사람이 검토 → 수동 반영" 흐름이 운영 리스크가 낮다.
- **운영 기준**: `POST /api/v1/ocr/extract`는 이미지/PDF를 받아 PaddleOCR 결과의 원문 텍스트, 줄별 신뢰도, 좌표를 반환한다. `GET /api/v1/ocr/health?warm=1`로 sidecar와 모델 로드를 사전 점검하고, `scripts/setup_ocr_sidecar.sh`로 Python 런타임을 재현 가능하게 설치한다. OCR 결과 자동 저장과 제조사별 좌표 파서는 실사용 서류 샘플이 충분히 쌓인 뒤 별도 TASK로 확장한다.
- **날짜**: 2026-04-30

## D-097: 아마란스 웹 출고 업로드 실물 양식 + RPA 작업 대기열
- **결정**: 출고 아마란스 export는 실제 웹 `출고등록엑셀업로드` 파일 구조에 맞춰 1행 한글헤더, 2행 ERP코드, 3행 타입/길이/필수 설명, 4행부터 데이터로 생성한다. 출고 담당자코드(`PLN_CD`)는 기본 `A001`, 관리구분(`MGMT_CD`)은 기본 `LS10`, 부가세포함단가(`VAT_UM`)는 실물 샘플과 동일하게 공란으로 둔다. 운영 값이 바뀌면 `AMARANTH_DEFAULT_PLN_CD`, `AMARANTH_OUTBOUND_MGMT_CD` 환경변수로 덮어쓴다.
- **이유**: 2026-04-30 아마란스 웹에서 내려받은 `출고등록엑셀업로드_ExcelUpload_20260430.xlsx`가 기존 SolarFlow export보다 3행 설명줄이 1행 더 있고, 샘플 데이터가 `A001`/`LS10`/`VAT_UM 공란` 정책으로 업로드되어 있었다. 실물 양식과 행 시작점이 다르면 RPA 파일 업로드 성공률이 낮아진다.
- **작업 대기열**: `amaranth_upload_jobs`에 생성 파일 경로, SHA-256, 상태(`pending/running/uploaded/failed/manual_required/cancelled`), 생성자, 시도 횟수, 업로드 결과 메시지를 저장한다. 동일한 `job_type + file_sha256`은 중복 작업을 만들지 않아 같은 엑셀 파일의 이중 업로드를 방지한다.
- **날짜**: 2026-04-30

## D-098: 면장 PDF OCR 자동채움은 B/L 폼 후보 반영 방식
- **결정**: 수입필증/면장 PDF는 `/api/v1/ocr/extract`에 `document_type=customs_declaration`을 함께 보내면 구조화 후보를 반환한다. B/L 입력/수정 폼은 이 후보로 B/L(AWB)번호, 입항일, 운송주선인, 국내도착항, 모델/규격, 수량, 단가, USD금액, CIF 원화금액, 환율을 채우되 즉시 DB 저장하지 않는다.
- **이유**: 면장 OCR은 수입 원가와 입고 수량에 직접 영향을 주므로 자동 저장은 위험하다. 다만 실무자가 매번 같은 칸을 옮겨 적는 부담이 크므로, “PDF 선택 → 입력칸 자동채움 → 사람이 확인 후 저장”으로 속도와 안정성을 함께 확보한다.
- **운영 기준**: 한글 OCR이 일부 깨지는 스캔본도 있으므로, 파서는 라벨 텍스트뿐 아니라 좌표와 숫자 패턴을 함께 사용한다. 수입자/무역거래처/HS/세관처럼 현재 B/L 본문에 직접 저장하지 않는 값은 저장 대신 참고 요약으로 표시한다.
- **날짜**: 2026-04-30

## D-099: 입고관리 화면 드래그는 입고등록 자동 진입으로 처리
- **결정**: 입고관리 화면에서 PDF/사진 파일을 드래그해 놓으면 별도 OCR 메뉴로 보내지 않고 입고등록 창을 바로 연다. 신규 폼은 해외직수입으로 자동 선택하고, OCR 후보 확인창을 띄운 뒤 사용자가 확인해야 입력칸에 반영한다.
- **이유**: 실무자는 면장 파일을 “등록할 입고자료”로 인식하므로, 먼저 입고등록을 열고 업로드 위치를 찾아가게 하는 흐름은 느리다. 화면 어디서나 파일을 놓으면 해당 업무 맥락으로 이동하는 편이 빠르고 직관적이다. 단, OCR 오류가 원가·수량에 영향을 줄 수 있으므로 자동 저장은 계속 금지한다.
- **운영 기준**: 드롭 대상은 PDF와 이미지 파일로 제한한다. 파일을 놓으면 입고등록 창이 열린 뒤 D-098과 같은 확인 후 반영 절차를 따른다. 폼 내부의 파일 선택/드롭 경로도 유지해 사용자가 이미 등록창에 들어와 있는 경우에도 같은 기능을 사용할 수 있다.
- **날짜**: 2026-04-30

## D-100: 아마란스 웹 업로드 자동화는 Playwright 워커 + 수동확인 안전장치로 시작
- **결정**: 아마란스 웹 업로드는 SolarFlow 내부 백엔드가 직접 브라우저를 띄우지 않고, 별도 Node/Playwright 워커(`rpa/amaranth-uploader`)가 로그인 세션을 재사용해 처리한다. 워커는 `pending` 작업을 `claim`으로 선점한 뒤 저장된 엑셀을 내려받아 `기능모음 → 엑셀 업로드 → 변환확인` 순서로 실행한다.
- **이유**: 아마란스는 외부 웹 UI라 로그인 세션, 팝업, 메뉴 문구, 화면 개편에 따라 자동화가 깨질 수 있다. 따라서 본격 배포 전 단계에서는 작은 워커로 분리해 관찰 가능성을 높이고, 성공 확신이 없으면 `uploaded`가 아니라 `manual_required`로 남겨 사람이 이어받게 한다.
- **운영 기준**: 최초 1회 `npm run login`으로 브라우저 프로필에 아마란스 세션을 저장한다. 이후 `npm run once` 또는 `npm run watch`가 작업을 처리하며, 실패/미확인 시 `artifacts/` 스크린샷 경로와 오류 코드를 `last_error`에 저장한다. SolarFlow 접근은 사용자 access token 또는 `/api/v1/export/amaranth/*`에만 허용되는 `SOLARFLOW_AMARANTH_RPA_TOKEN`으로 한다. 세션 만료가 잦으면 자동화 전용 PC에서만 `AMARANTH_AUTO_LOGIN=true`와 아이디/비밀번호 환경변수를 사용해 로그인 fallback을 허용한다. 화면 문구 변경은 `.env`의 정규식으로 조정한다.
- **날짜**: 2026-04-30

## D-101: PR19 정적 목업은 실제 프론트 디자인 소스로 포팅
- **결정**: PR19의 `frontend/mockups/v3` 정적 HTML/Babel JS/CSS를 iframe으로 끼우지 않고, CSS와 컴포넌트 구조를 실제 React/TypeScript 화면으로 포팅한다. 초기에는 목업 디자인을 최대한 그대로 옮기고, 이후 실제 API·권한·업무 흐름에 맞춰 화면 구조를 점진적으로 조정한다.
- **이유**: PR19 목업은 화면 밀도, 좌측 내비게이션, KPI 타일, 우측 업무 레일 등 운영 콘솔의 방향성이 잘 잡혀 있지만, 더미 데이터와 전역 React/Babel 실행 방식이라 그대로 넣으면 인증, 라우팅, 권한, API 상태와 분리된다. 실제 앱 코드로 옮겨야 디자인과 기능이 함께 진화할 수 있다.
- **운영 기준**: 공통 목업 부품은 `MockupPrimitives`로 먼저 분리하고, 각 화면은 기존 데이터 훅과 저장 로직을 유지한 채 레이아웃부터 이관한다. 새 디자인 때문에 저장/검증/권한 동작을 약화하지 않는다.
- **날짜**: 2026-04-30

## D-102: PR19 내비게이션 배지는 운영 알림 훅을 정본으로 사용
- **결정**: PR19 좌측 사이드바의 숫자 배지는 목업 고정값을 제거하고 `useAlerts`가 계산한 실제 운영 알림 건수에서 파생한다. 대시보드 우측 레일도 같은 알림·미착품·수주잔량 데이터를 사용해 워크큐를 표시한다.
- **이유**: 목업 숫자가 남아 있으면 사용자가 화면을 상태판으로 신뢰하기 어렵다. 실제 API 데이터와 같은 조건에서 계산된 배지만 보여야 알림 벨, 사이드바, 대시보드가 서로 다른 말을 하지 않는다.
- **운영 기준**: 미수금 알림은 현재 Rust `customer-analysis`의 `items` 응답 스키마를 기준으로 하되, 과거 `customers` 형태도 허용한다. 계산서 미발행은 출고에 매출이 없거나 매출은 있어도 세금계산서 발행일이 없는 경우로 판단한다.
- **날짜**: 2026-05-01

## D-103: PR19 모바일 셸은 하단 아이콘 도크로 전환
- **결정**: 640px 이하 화면에서는 PR19 좌측 사이드바를 화면 왼쪽에 유지하지 않고, 하단 가로 아이콘 도크로 전환한다. 대시보드 KPI와 테이블은 좁은 화면에서 1열/가로 스크롤로 접는다.
- **이유**: 모바일 폭에서 72px 좌측 레일을 계속 유지하면 데이터 표와 KPI 카드가 너무 좁아져 운영자가 값을 읽기 어렵다. 하단 도크는 메뉴 접근성을 유지하면서 본문 폭을 확보한다.
- **운영 기준**: 메뉴 라벨은 모바일 도크에서 숨기되 아이콘과 알림 배지는 유지한다. 본문 테이블은 정보를 카드로 재해석하지 않고 원래 표 구조를 보존한 채 가로 스크롤을 허용한다.
- **날짜**: 2026-05-01

## D-104: OCR 품목 후보는 확인창에서 사람이 확정
- **결정**: 면장 OCR 확인창은 모델/규격 라인마다 실제 품번 후보를 함께 보여주고, 자동 후보가 맞지 않으면 사용자가 드롭다운에서 품번을 바꾼 뒤 입력칸에 반영한다.
- **이유**: OCR 모델명은 제조사·세관 양식마다 표기가 달라서 자동 매칭만으로 입고 품목을 확정하면 재고와 원가가 틀어질 수 있다. 하지만 후보를 눈앞에 보여주고 사람이 확정하면 자동채움 속도와 운영 안전성을 함께 얻을 수 있다.
- **운영 기준**: 후보는 품번코드/품번명, 출력(Wp), OCR 모델 토큰으로 정렬한다. 수입자·무역거래처 후보는 PO 연결값이 없을 때만 법인/공급사로 반영하고, PO 연결값이 있으면 참고값으로만 둔다.
- **날짜**: 2026-05-01
