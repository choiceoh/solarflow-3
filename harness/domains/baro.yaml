schema_version: 1

id: baro
display_name: "바로 도메인 (영업/CRM/배송)"

pack: baro-domain
feature_id: baro.domain
visible_to: baro

paths:
  backend:
    - backend/internal/domains/baro/

tables: []
depends_on: []
external_users: []

blast_radius:
  - description: "baro model 필드 변경"
    must_check:
      - "backend/internal/domains/baro/handler.go Validation"
      - "backend/migrations/ + check_schema.sh"

verify_scripts:
  - command: scripts/verify_changed.sh
    when: always
  - command: backend/scripts/check_schema.sh
    when: model_or_migration_changed

owners: [choiceoh]
decisions: []
